- name: Playbook Condition
  template:
    src: mytemplate.j2
    dest: /tmp/mytemplatej2.txt
  when: accion == "instalar" and (ansible_distribution != "Debian" and ansible_distribution == "Ubuntu")
